# Fichier : netlify.toml

[functions]
  # Indique à Netlify où se trouvent vos fonctions
  directory = "netlify/functions/"

# Cette règle s'applique à TOUTES les fonctions
# et force l'inclusion du dossier "private" et de tout son contenu.
[[functions."*"]]
  included_files = ["private/**"]
