# The /* rule applies these headers to all pages on your site.
/*
  # Content Security Policy: This is the most important header. It acts as a firewall,
  # telling the browser exactly what resources (scripts, styles, images) are allowed to load.
  # This policy prevents Cross-Site Scripting (XSS) attacks.
  # We have added 'https://www.google.com' to frame-src to allow Google Maps.
  Content-Security-Policy: default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self'; object-src 'none'; frame-src 'self' https://www.google.com;
  
  # X-Frame-Options: Prevents your site from being put in an <iframe> on another site (clickjacking).
  X-Frame-Options: DENY
  
  # X-Content-Type-Options: Prevents the browser from guessing file types, which can be a security risk.
  X-Content-Type-Options: nosniff
  
  # Referrer-Policy: Controls how much information is sent when a user clicks a link to another site.
  Referrer-Policy: strict-origin-when-cross-origin
  
  # Permissions-Policy: Lets you control which browser features can be used on your site.
  # This example disables the camera, microphone, and geolocation features.
  Permissions-Policy: camera=(), microphone=(), geolocation=()
